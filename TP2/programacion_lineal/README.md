# PROBLEMA 1 - Programación Lineal

Instrucciones:

1. Instalar dependencias:

```bash
pip install -r requirements.txt
```

2. Ejecutar el solver:

```bash
python3 main.py
```

El resultado se guardará en `resultados.txt`.
